<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, maximum-scale=1.0, minimum-scale=1.0">
    <title>智慧餐饮 - AI健康顾问</title>
    <!-- 防缓存设置 -->
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <link rel="stylesheet" href="../static/css/all.min.css">
    <style>
        /* ===== 移动端全屏布局专用样式 ===== */
        :root {
            --primary-deep: #1a5f9e;
            --primary-dark: #0d3a6b;
            --primary-light: #2c72b5;
            --secondary: #2c3e50;
            --surface: #ffffff;
            --surface-soft: #f8fafc;
            --surface-hover: #eef5fc;
            --text-primary: #2c3e50;
            --text-secondary: #546e7a;
            --text-muted: #78909c;
            --accent: #3498db;
            --success: #27ae60;
            --warning: #f39c12;
            --shadow-soft: 0 2px 8px rgba(26, 95, 158, 0.08);
            --shadow-card: 0 4px 16px rgba(26, 95, 158, 0.12);
            --shadow-hover: 0 8px 24px rgba(26, 95, 158, 0.15);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'PingFang SC', 'Helvetica Neue', Arial, sans-serif;
        }

        html {
            height: 100%;
        }

        body {
            background: linear-gradient(145deg, #eef5fc 0%, #e6f2ff 100%);
            color: var(--text-primary);
            font-size: 15px;
            height: 100vh;
            width: 100vw;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        /* ===== 全屏容器布局 ===== */
        .container {
            width: 100%;
            height: 100vh;
            padding: 0;
            background: transparent;
            position: relative;
        }

        /* ===== iframe容器 - 占满整个屏幕，但底部70px区域不接收点击事件 ===== */
        .chat-iframe-container {
            width: 100vw;
            height: 100vh;
            padding: 0;
            margin: 0;
            position: absolute;
            top: 0;
            left: 0;
            z-index: 1;
            /* 使用clip-path来避免底部70px区域接收点击事件 */
            clip-path: inset(0 0 70px 0);
        }

        .iframe-wrapper {
            width: 100%;
            height: 100%;
            background: var(--surface-soft);
            position: relative;
            overflow: hidden;
            border: none;
            box-shadow: none;
            outline: none;
        }

        .iframe-wrapper iframe {
            width: 100%;
            height: 100%;
            border: none;
            outline: none;
            box-shadow: none;
            display: block;
        }

        /* ===== 简化的浮动顶部栏 - 覆盖在iframe之上 ===== */
        .top-header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 60px;
            background: linear-gradient(135deg, var(--primary-deep), var(--primary-dark));
            color: white;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            padding: 0 15px;
            z-index: 2000;
            box-shadow: var(--shadow-card);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .back-button {
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
            text-decoration: none;
            color: white;
        }

        .back-button:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.05);
        }

        .header-title {
            font-size: 1.1rem;
            font-weight: 700;
        }

        /* ===== 底部导航栏样式 - 覆盖在iframe之上 ===== */
        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 70px;
            background: var(--surface);
            border-top: 1px solid rgba(26, 95, 158, 0.08);
            display: flex;
            justify-content: space-around;
            align-items: center;
            z-index: 2001;
            box-shadow: 0 -4px 16px rgba(26, 95, 158, 0.12);
            backdrop-filter: blur(10px);
            /* 确保可以接收点击事件 */
            pointer-events: auto;
        }

        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 8px 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 12px;
            min-width: 60px;
            position: relative;
            /* 确保导航项可以接收点击事件 */
            pointer-events: auto;
            /* 增加点击区域 */
            min-height: 50px;
        }

        .nav-item:hover {
            background: var(--surface-hover);
            transform: translateY(-2px);
        }

        .nav-item.active {
            background: linear-gradient(135deg, rgba(26, 95, 158, 0.1), rgba(26, 95, 158, 0.05));
            border: 1px solid rgba(26, 95, 158, 0.2);
        }

        .nav-icon {
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: var(--text-muted);
            margin-bottom: 4px;
            transition: all 0.3s ease;
            pointer-events: none; /* 防止图标阻止点击事件 */
        }

        .nav-item.active .nav-icon {
            color: var(--primary-deep);
            transform: scale(1.1);
        }

        .nav-label {
            font-size: 0.75rem;
            color: var(--text-muted);
            font-weight: 500;
            transition: all 0.3s ease;
            pointer-events: none; /* 防止文字阻止点击事件 */
        }

        .nav-item.active .nav-label {
            color: var(--primary-deep);
            font-weight: 600;
        }

        /* 加载状态 */
        .loading-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            z-index: 10;
        }

        .loading-spinner {
            width: 40px;
            height: 40px;
            border: 3px solid rgba(26, 95, 158, 0.1);
            border-top: 3px solid var(--primary-deep);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 15px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .loading-text {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        /* 确保在所有移动设备上都是全屏 */
        @media screen and (max-width: 768px) {
            html, body {
                height: 100vh;
                height: -webkit-fill-available;
            }
            
            .container {
                height: 100vh;
                height: -webkit-fill-available;
            }
            
            .chat-iframe-container {
                height: 100vh;
                height: -webkit-fill-available;
            }
            
            .nav-item {
                min-height: 50px;
                padding: 10px 8px;
            }
        }

        /* 横屏适配 */
        @media screen and (orientation: landscape) and (max-height: 500px) {
            .top-header {
                height: 50px;
            }
        }
    </style>
</head>
<body>
    <!-- 全屏容器 -->
    <div class="container">
        <!-- iframe容器 - 占满整个屏幕 -->
        <div class="chat-iframe-container">
            <div class="iframe-wrapper" id="iframeWrapper">
                <!-- 加载状态 -->
                <div class="loading-container" id="loadingContainer">
                    <div class="loading-spinner"></div>
                    <div class="loading-text">正在加载AI健康顾问...</div>
                </div>

                <!-- 聊天机器人配置 -->
                <iframe 
                    id="chatbotIframe"
                    src=""
                    style="display: none;"
                    onload="handleIframeLoad()"
                    onerror="handleIframeError()">
                </iframe>
            </div>
        </div>
    </div>

    <!-- 简化的浮动顶部栏 - 覆盖在iframe之上 -->
    <div class="top-header visible" id="topHeader">
        <div class="header-left">
            <a href="/" class="back-button" title="返回首页">
                <i class="fas fa-arrow-left"></i>
            </a>
            <div class="header-title">AI健康顾问</div>
        </div>
    </div>

    <!-- 底部导航栏 - 覆盖在iframe之上 -->
    <div class="bottom-nav" id="bottomNav">
        <div class="nav-item" data-target="index">
            <div class="nav-icon">
                <i class="fas fa-home"></i>
            </div>
            <div class="nav-label">主页</div>
        </div>
        <div class="nav-item" data-target="orders">
            <div class="nav-icon">
                <i class="fas fa-utensils"></i>
            </div>
            <div class="nav-label">点餐</div>
        </div>
        <div class="nav-item" data-target="repo">
            <div class="nav-icon">
                <i class="fas fa-chart-line"></i>
            </div>
            <div class="nav-label">报告</div>
        </div>
        <div class="nav-item active" data-target="ai-assistant">
            <div class="nav-icon">
                <i class="fas fa-robot"></i>
            </div>
            <div class="nav-label">AI助手</div>
        </div>
    </div>

    <script>
        // 全屏移动端优化脚本
        let loadTimeout;

        // 生成防缓存的iframe URL
        function generateIframeUrl() {
            const baseUrl = "http://192.168.101.251/chatbot/wQy2WHP8yGcCy7Fc";
            const timestamp = new Date().getTime();
            const randomId = Math.random().toString(36).substr(2, 9);
            return `${baseUrl}?t=${timestamp}&r=${randomId}`;
        }

        // 处理iframe加载成功
        function handleIframeLoad() {
            console.log('iframe加载成功');
            clearTimeout(loadTimeout);
            
            const loadingContainer = document.getElementById('loadingContainer');
            const iframe = document.getElementById('chatbotIframe');
            
            if (loadingContainer) loadingContainer.style.display = 'none';
            if (iframe) {
                iframe.style.display = 'block';
            }
        }

        // 处理iframe加载错误
        function handleIframeError() {
            console.log('iframe加载失败');
            clearTimeout(loadTimeout);
            
            const loadingContainer = document.getElementById('loadingContainer');
            const iframe = document.getElementById('chatbotIframe');
            
            if (loadingContainer) loadingContainer.style.display = 'none';
            if (iframe) {
                iframe.style.display = 'block';
            }
        }

        // 底部导航点击跳转
        function initBottomNavigation() {
            console.log('初始化底部导航栏');
            const navItems = document.querySelectorAll('.nav-item');
            console.log('找到导航项数量:', navItems.length);
            
            navItems.forEach((item, index) => {
                const target = item.getAttribute('data-target');
                console.log(`绑定导航项 ${index}:`, target);
                
                // 清除之前的事件监听器
                item.onclick = null;
                item.ontouchend = null;
                
                // 添加点击事件
                item.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    console.log('点击导航项:', target);
                    
                    let targetUrl = '';
                    switch(target) {
                        case 'index':
                            targetUrl = "/";
                            break;
                        case 'orders':
                            targetUrl = "/orders/";
                            break;
                        case 'repo':
                            targetUrl = "/repo/";
                            break;
                        case 'ai-assistant':
                            targetUrl = "/ai_health_advisor/";
                            break;
                    }
                    
                    console.log('跳转到:', targetUrl);
                    
                    if (targetUrl && targetUrl !== "/ai_health_advisor/") {
                        // 添加时间戳防缓存
                        const separator = targetUrl.includes('?') ? '&' : '?';
                        const timestampUrl = `${targetUrl}${separator}t=${new Date().getTime()}`;
                        window.location.href = timestampUrl;
                    }
                });
                
                // 添加触摸事件支持（移动端）
                item.addEventListener('touchend', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    const target = this.getAttribute('data-target');
                    console.log('触摸导航项:', target);
                    
                    let targetUrl = '';
                    switch(target) {
                        case 'index':
                            targetUrl = "/";
                            break;
                        case 'orders':
                            targetUrl = "/orders/";
                            break;
                        case 'repo':
                            targetUrl = "/repo/";
                            break;
                        case 'ai-assistant':
                            targetUrl = "/ai_health_advisor/";
                            break;
                    }
                    
                    if (targetUrl && targetUrl !== "/ai_health_advisor/") {
                        setTimeout(() => {
                            // 添加时间戳防缓存
                            const separator = targetUrl.includes('?') ? '&' : '?';
                            const timestampUrl = `${targetUrl}${separator}t=${new Date().getTime()}`;
                            window.location.href = timestampUrl;
                        }, 100);
                    }
                });
            });
        }

        // 页面加载完成后初始化
        window.addEventListener('load', function() {
            console.log('页面加载完成');
            
            // 设置iframe的src为防缓存URL
            const iframe = document.getElementById('chatbotIframe');
            if (iframe) {
                iframe.src = generateIframeUrl();
            }
            
            // 设置加载超时
            loadTimeout = setTimeout(() => {
                handleIframeError();
            }, 15000);
            
            // 初始化底部导航栏
            setTimeout(initBottomNavigation, 100);
        });

        // 页面卸载前的处理 - 防缓存
        window.addEventListener('beforeunload', function() {
            console.log('页面即将卸载，清理缓存');
            // 清理iframe缓存
            const iframe = document.getElementById('chatbotIframe');
            if (iframe) {
                iframe.src = 'about:blank';
            }
        });

        // 页面隐藏时的处理（移动端切换应用时）
        document.addEventListener('visibilitychange', function() {
            if (document.hidden) {
                console.log('页面隐藏，准备刷新');
                // 当页面隐藏时，标记需要刷新
                sessionStorage.setItem('needRefresh', 'true');
            } else {
                console.log('页面显示');
                // 当页面重新显示时，检查是否需要刷新
                if (sessionStorage.getItem('needRefresh') === 'true') {
                    sessionStorage.removeItem('needRefresh');
                    // 重新加载iframe内容
                    const iframe = document.getElementById('chatbotIframe');
                    if (iframe) {
                        iframe.src = generateIframeUrl();
                    }
                }
            }
        });

        // DOM加载完成后也初始化一次
        document.addEventListener('DOMContentLoaded', function() {
            console.log('DOM加载完成');
            initBottomNavigation();
        });

        // 屏幕方向变化时重新初始化
        window.addEventListener('orientationchange', function() {
            setTimeout(initBottomNavigation, 500);
        });
    </script>
</body>
</html>